[project]
name = "rete-algorithm"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
  "pyrefly",
  "ruff",
  "pytest>=9.1.0",
  "pytest-xdist",
  "pytest-testmon",
  "hypothesis",
  "syrupy",
]

[tool.pytest.ini_options]
testpaths = ["rete/tests"]
